package pieces;

import init.field;

public class pawn implements piece{

    boolean firstMove = true;


    @Override
    public boolean moveTo(int row, int column) {

        return false;
    }
}
